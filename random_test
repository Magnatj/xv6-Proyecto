#include "types.h"
#include "user.h"

int main(int argc, char** argv)
{
	if(random_set(atoi(argv[i]))<0)
	{
		printf(1, "%f\n", randomValue());
	}

	else
	{
		printf(2, "random_test: Could not run set");
	}

	exit();
}
